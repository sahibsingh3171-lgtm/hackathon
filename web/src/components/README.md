# `components/` — React UI

| Subfolder | Contents |
|-----------|----------|
| `clarity/` | Product-specific Clarity UI (steps, matches, crisis, AI panels) |
| `intake/` | Intake wizard pieces shared with flow |
| `layout/` | App shell, providers, global chrome |
| `marketing/` | Landing page sections |
| `ui/` | shadcn-style primitives (Button, Card, …) |

**Rule of thumb:** pages in `app/` orchestrate data and navigation; **presentation** and reusable widgets live here.

Map: [`../../docs/CODEMAP.md`](../../docs/CODEMAP.md).
