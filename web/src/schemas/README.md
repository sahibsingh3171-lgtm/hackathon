# `schemas/` — JSON Schema artifacts

Reference schemas for structured outputs (e.g. readiness analysis). Runtime validation often lives in `lib/ai/*.contract.ts` (Zod); this folder can mirror or document the same contract for tooling.

Map: [`../../docs/CODEMAP.md`](../../docs/CODEMAP.md).
