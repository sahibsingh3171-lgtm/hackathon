# `hooks/` — Client-side React hooks

Example: `use-voice-dictation.ts` — microphone capture / streaming behavior for brain dump and practice flows.

**Secrets:** hooks run in the browser; any API keys stay in `app/api/` routes.

Map: [`../../docs/CODEMAP.md`](../../docs/CODEMAP.md).
