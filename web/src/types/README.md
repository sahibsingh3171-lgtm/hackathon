# `types/` — Shared TypeScript types

Cross-cutting interfaces: `clarity.ts` (`ClaritySession`, intake, lifestyle, match prefs), `readiness-analysis.ts`, etc.

**Single place** to answer “what shape is session state?”

Map: [`../../docs/CODEMAP.md`](../../docs/CODEMAP.md).
