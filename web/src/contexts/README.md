# `contexts/` — React context providers

| File | Role |
|------|------|
| `clarity-session-context.tsx` | **Single source of client session state** for the Clarity flow (`ClaritySession`), `setSession`, demo reset |

Server persistence rules live in `src/lib/clarity/session.ts` and `persisted-session.ts`.

Map: [`../../docs/CODEMAP.md`](../../docs/CODEMAP.md).
