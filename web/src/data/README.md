# `data/` — Static mock datasets

TypeScript modules that export **demo therapist profiles**, builders, and taxonomy constants used by matching and `MatchFilters`.

Not fetched from a live API — safe for hackathon judging and deterministic ranking.

Map: [`../../docs/CODEMAP.md`](../../docs/CODEMAP.md).
