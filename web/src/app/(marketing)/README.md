# `(marketing)/` — public landing

- **`page.tsx`** — composes homepage sections from `src/components/marketing/`.
- **`layout.tsx`** — marketing-specific layout wrapper.

This route group keeps marketing separate from the `(flow)` wizard for clarity and styling.

Full map: [`../../../docs/CODEMAP.md`](../../../docs/CODEMAP.md).
