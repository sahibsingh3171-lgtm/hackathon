/**
 * Therapist **matching, ranking, and sort modes** (deterministic TS).
 * Filter **UI** and prefs state: see `README.md`. Mock rows: `@/features/therapist-directory`.
 */
export * from "@/lib/therapist";
